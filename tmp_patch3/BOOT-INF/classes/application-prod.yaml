--- #################### 注册中心 + 配置中心相关配置 ####################

spring:
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:localhost:8848} # Nacos 服务器地址
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      discovery: # 【配置中心】配置项
        namespace: prod # 命名空间。这里使用 dev 开发环境
        group: DEFAULT_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP
        register-enabled: true # 是否开启服务注册
      config: # 【注册中心】配置项
        namespace: prod # 命名空间。这里使用 dev 开发环境
        group: DEFAULT_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP

# 日志文件配置
logging:
  level:
    org.springframework.context.support.PostProcessorRegistrationDelegate: ERROR # TODO 芋艿：先禁用，Spring Boot 3.X 存在部分错误的 WARN 提示
# 创蓝一键登录的配置
chuanglan:
  flash:
    ios:
      app-id: mBSYB4ll
      app-key: UDbqkiUY
    android:
      app-id: sFAX4BpZ
      app-key: do4mKGhz
    query-url: https://api.253.com/open/flashsdk/mobile-query

apple:
  auth:
    url: https://appleid.apple.com/auth/keys
  iss:
    url: https://appleid.apple.com
  client:
    id: com.shenyu.sports

app:
  wxAuth:
    url: https://h5.shenyu.com/pages/plan/planInfo?id=%s
    newsUrl: https://h5.shenyu.com/app-api/match/news/get?id=%s
