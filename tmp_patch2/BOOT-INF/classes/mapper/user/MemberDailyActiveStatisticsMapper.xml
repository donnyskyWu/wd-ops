<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="football.module.member.dal.mysql.user.MemberDauStatMapper">

    <insert id="insertOrUpdate">
        INSERT INTO member_dau_stat (tenant_id, biz_date, active_count, create_time, update_time)
        VALUES (#{tenantId}, #{bizDate}, #{activeCount}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
            active_count = VALUES(active_count),
            update_time = NOW()
    </insert>

</mapper>