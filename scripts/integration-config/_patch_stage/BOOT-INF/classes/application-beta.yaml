--- #################### 注册中心 + 配置中心相关配置 ####################
# 配置文件放nacos中
# 配置文件放nacos中
# 配置文件放nacos中
spring:
  cloud:
    nacos:
      server-addr: 110.42.49.224:8848 # Nacos 服务器地址
      username: nacos
      password: nacos
      discovery: # 【配置中心】配置项
        namespace: beta # 命名空间。这里使用 dev 开发环境
        group: DEFAULT_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP
        register-enabled: true # 是否开启服务注册
      config: # 【注册中心】配置项
        namespace: beta # 命名空间。这里使用 dev 开发环境
        group: DEFAULT_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP
