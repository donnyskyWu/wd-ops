# Nacos 发布模板 — DataId: ops-server-beta.yaml · Namespace: beta · Group: DEFAULT_GROUP
# 复制到 Nacos 配置中心并填入真实口令；勿提交含密文副本到 git
# JAR Bootstrap：application-beta.yaml（Nacos 连接）
# jingcai / 数据采集：application.yaml 公共段（勿在本文件重复，避免覆盖）

--- #################### 数据库 + Redis ####################

spring:
  flyway:
    enabled: false
  datasource:
    druid:
      web-stat-filter:
        enabled: true
      stat-view-servlet:
        enabled: true
        allow:
        url-pattern: /druid/*
        login-username:
        login-password:
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-millis: 100
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
    dynamic:
      primary: master
      strict: true
      druid:
        initial-size: 1
        min-idle: 1
        max-active: 20
        max-wait: 60000
        time-between-eviction-runs-millis: 60000
        min-evictable-idle-time-millis: 600000
        max-evictable-idle-time-millis: 1800000
        validation-query: SELECT 1 FROM DUAL
        test-while-idle: true
        test-on-borrow: false
        test-on-return: false
        pool-prepared-statements: true
        max-pool-prepared-statement-per-connection-size: 20
      datasource:
        master:
          url: jdbc:mysql://110.42.49.224:3306/shenyu-ops?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&rewriteBatchedStatements=true
          username: shenyu-ops
          password: xxx
          driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 110.42.49.224
      port: 6379
      database: 1
      password: xxx

--- #################### 定时任务 + 分布式锁 ####################

xxl:
  job:
    enabled: false
    admin:
      addresses: http://127.0.0.1:9090/xxl-job-admin

lock4j:
  acquire-timeout: 3000
  expire: 30000

--- #################### 监控端点 ####################

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

--- #################### Ops / Football 业务（Beta） ####################

football:
  web:
    admin-ui:
      url: https://beta.h5.shenyu.com

oa:
  auth:
    gateway-login-user:
      enabled: true
    dev-token:
      enabled: false
    legacy-ds-token:
      enabled: false
    football-redis:
      enabled: true
      host: 110.42.49.224
      port: 6379
      database: 1
      password: xxx
  crypto:
    aes-key: xxx
