# bpm-server beta master DS (Football only; OPS oa-server has no bpm dynamic DS today).
# Use with football-integration-overlay-beta.yml + ops-test-remote.env
spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://${OPS_TEST_DB_HOST:110.42.49.224}:${OPS_TEST_DB_PORT:3306}/${OPS_TEST_BPM_DB:shenyu-bpm}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&rewriteBatchedStatements=true
          username: ${OPS_TEST_BPM_USER:shenyu-bpm}
          password: ${OPS_TEST_BPM_PASSWORD:CHANGE_ME}
        slave:
          url: jdbc:mysql://${OPS_TEST_DB_HOST:110.42.49.224}:${OPS_TEST_DB_PORT:3306}/${OPS_TEST_BPM_DB:shenyu-bpm}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&rewriteBatchedStatements=true
          username: ${OPS_TEST_BPM_USER:shenyu-bpm}
          password: ${OPS_TEST_BPM_PASSWORD:CHANGE_ME}
