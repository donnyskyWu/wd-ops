# DEPRECATED thin pointer — SSOT moved to classpath application-dev-test-beta.yaml
# (football-module-ops-server/src/main/resources/application-dev-test-beta.yaml)
#
# Preferred activation:
#   java -jar football-module-ops-server.jar --spring.profiles.active=dev-test-beta
#   .\scripts\start-integration-oa.ps1 -Profiles "dev-test-beta"
#   .\scripts\start-ops-dev.ps1 -Beta
#
# Load secrets from ops-test-remote.env before start.
# See docs/delivery/OPS-TEST-DB.md · docs/delivery/OPS-TEST-SERVER-DEPLOY-GUIDE.md §6.2
#
# This file is kept for backward compatibility only. If you must use additional-location:
#   --spring.config.additional-location=optional:file:scripts/integration-config/ops-test-beta-multidb.yml
# content below mirrors the jar profile (no Feign URLs — add via local profile or override).

spring:
  profiles:
    include: dev-test-beta
