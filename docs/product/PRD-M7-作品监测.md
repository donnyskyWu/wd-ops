# PRD-M7-作品监测

> **业务域**：M7 作品监测
> **功能模块**：外部账号分析 + 监测
> **详细设计章节**：5.32、5.33
> **版本**：v1.1 | 2026-06-11
> **状态**：Draft（实现已对齐）
> **全局规范**：[`docs/engineering/GLOBAL-CONVENTIONS.md`](./../engineering/GLOBAL-CONVENTIONS.md)

---

## 0. 元信息

| 字段 | 值 |
|------|---|
| 模块 | M7 作品监测 |
| 业务域 | 作品监测（MONITOR） |
| 详细设计 | `## 5.32~5.33` |

---

## 1. 概述

### 1.1 一句话描述

监测**外部竞品平台**的账号动态、爆款作品、低分作品、高/低粉账号，提供 IP 主题和行业维度的数据聚合。

### 1.2 目标

- 替代 Excel 手动统计
- 1 分钟内发现竞品爆款
- 高/低粉账号精准预测

### 1.3 术语表

| 术语 | 定义 |
|------|------|
| **外部账号** | 竞品平台账号（非本系统管理的） |
| **高粉账号** | 粉丝增速 ≥ 阈值 |
| **低粉账号** | 粉丝连续 7 天下降 |
| **爆款作品** | BR-003 阈值（播放 ≥ 100w） |
| **低分作品** | BR-004 阈值（完播率 < 20%） |
| **IP 主题分析** | 按 IP 主题聚合外部数据 |
| **行业分析** | 按行业聚合外部数据 |

---

## 2. 范围

### 2.1 In Scope（2 个 FR 模块）

| FR 编号 | 名称 | 优先级 | 详细设计 |
|---------|------|--------|---------|
| FR-M7-001 | 外部作品监测（6 大维度） | P0 | 5.32 |
| FR-M7-002 | 高/低粉账号 + IP 主题 + 行业分析 | P0 | 5.33 |

### 2.2 Out of Scope

1. ❌ **不实现** 内部作品监测（属于 `## 5.5 作品分析`）
2. ❌ **不实现** 竞品账号的实时数据抓取（依赖 `## 5.40 数据采集`）

---

## 3. 功能需求

### FR-M7-001 外部作品监测（5.32）

#### 4.1.1 描述

监测外部竞品平台的账号动态、爆款/低分作品，提供 6 大维度分析。

#### 4.1.2 数据项

| 字段 | 控件 | 字典/实体 |
|------|------|----------|
| `platformType` | `<DictSelect dict-type="dict_platform_type" />` | 字典 |
| `ipGroupId` | `<IpGroupTreeSelect />` | `oa_ip_group` |
| `industry` | `<Select />` | 固定值（娱乐/财经/科技/教育/...） |
| `startDate` / `endDate` | `<DateRangePicker />` | - |
| `accountId` | `<AccountSelect />` | `oa_account`（外部） |

#### 4.1.3 业务规则

- 爆款阈值：BR-003 = 100w 播放
- 低分阈值：BR-004 = 完播率 < 20%
- 高粉：日增粉丝 ≥ 阈值
- 低粉：连续 7 天下降

#### 4.1.4 验收标准

**AC-M7-001-1**（外部账号列表）
**AC-M7-001-2**（爆款作品）
**AC-M7-001-3**（低分作品）
**AC-M7-001-4**（高/低粉账号）
**AC-M7-001-5**（平台/IP/行业 筛选）

#### 4.1.5 实现补充（2026-06-11）

以下 5 个分析页表格行增加「详情」按钮 + 右侧抽屉（`el-drawer` 480px），展示当前行关键字段只读摘要：

| 页面 | 文件 |
|------|------|
| 外部账号分析 | `ExternalAccountAnalysis.vue` |
| 爆款作品分析 | `HotWorksAnalysis.vue` |
| 低分作品分析 | `LowScoreAnalysis.vue` |
| 高粉账号分析 | `HighFansAccountAnalysis.vue` |
| 低粉账号分析 | `LowFansAccountAnalysis.vue` |

---

### FR-M7-002 高/低粉账号 + IP 主题 + 行业（5.33）

#### 4.2.1 描述

按 IP 主题/行业维度聚合外部账号数据。

#### 4.2.2 验收标准

**AC-M7-002-1**（高粉账号列表）
**AC-M7-002-2**（低粉账号列表 + 预警）
**AC-M7-002-3**（IP 主题聚合）
**AC-M7-002-4**（行业聚合）

---

## 4. 集成与数据

### 4.1 核心实体

| 实体 | 用途 |
|------|------|
| `oa_external_account` | 外部账号 |
| `oa_external_content` | 外部作品 |
| `oa_external_follower_daily` | 外部粉丝每日数据 |
| `oa_ip_theme` | IP 主题（字典） |
| `oa_industry` | 行业（字典） |

### 4.2 关联属性

| 字段 | 选择器 |
|------|--------|
| `ipGroupId` | `<IpGroupTreeSelect />` |
| `accountId` | `<AccountSelect />` |
| `platformType` | `<DictSelect dict-type="dict_platform_type" />` |

### 4.3 数据来源

- 依赖 `## 5.40 数据采集` 拉取
- 若采集未启动 → 显示"暂无数据" + 提示开启采集

---

## 5. 决策记录

| 编号 | 问题 | 决策 | 原因 |
|------|------|------|------|
| ADR-M7-001 | 爆款阈值是多少？ | 100w 播放（BR-003） | 业务经验 |
| ADR-M7-002 | 低分阈值 | 完播率 < 20%（BR-004） | 行业标准 |

---

*下一步：UX / API / STATE / SLICES / CHECKLIST / TESTCASES。*
