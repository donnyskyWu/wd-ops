# G-PAY-01 + G-SYS-02 Integration 手验记录

| 字段 | 值 |
|------|---|
| 日期 | 2026-07-29 |
| 环境 | dev-nacos-local · localhost 五库 |
| 执行人 | AI P0 todo |
| 关联 | [OPS-FOOTBALL-INTEGRATION-FEIGN-CHECKLIST §5](../OPS-FOOTBALL-INTEGRATION-FEIGN-CHECKLIST.md) |

---

## 1. 前置变更

| 项 | 处理 |
|----|------|
| pay-server 未纳入 Integration 脚本 | `start-integration-all.ps1` 新增 :48085 启动 + `pay-integration-overlay.yml` |
| pay-server JAR 未构建 | `mvn -pl football-module-pay/football-module-pay-server -am package -DskipTests` |
| pay-server 启动 | :48085 LISTEN（`scripts/logs/pay-server-integration.log`） |

## 2. G-PAY-01

| 抽检 | 结果 | 备注 |
|------|------|------|
| `POST :48085/rpc-api/pay/order/page` Body `{"pageNo":1,"pageSize":10}` + `tenant-id:1` | **HTTP 200 · code 500** | pay-server 内调 `member-server` `GET /rpc-api/member/author/permitted-ids` → Connection refused（:48087 DOWN） |
| `GET :48080/admin-api/oa/football-order/list?...` + `Bearer dev-token-oa-admin` + `X-Tenant-Id:1` | **HTTP 200 · code 403** | dev-token 可达 Gateway；缺 `oa:order-attribution:list` / `oa:roi:list` 权限 |

**结论**：pay-server **进程 cutover ✅**；G-PAY-01 **业务手验仍阻塞** — 须 member-server :48087 UP（`integration-member-stub` classpath 与 member-server 期望的 `MemberIntegrationRocketMqStubConfiguration` 不一致，启动失败）。

## 3. G-SYS-02

| 抽检 | 结果 | 备注 |
|------|------|------|
| `GET :48081/rpc-api/system/user/simple-list` | **HTTP 200 · code 0** | Feign 目标可达 |
| `GET :48081/rpc-api/system/permission/has-any-roles?userId=1&roles=super_admin` | **HTTP 200 · code 0 · data=false** | 角色 RPC 可达 |
| `GET :48081/rpc-api/system/user/get?id={snowflake}` | **HTTP 200 · code 500** | RPC 端点可达；id 语义/上下文待对齐 |
| IP 组保存 OPS 业务步骤 | **待手验** | `IpGroupServiceImpl` → `FootballSystemUserValidator` Feign-only 已 cutover；需 UI/curl 保存 + oa 日志 |

## 4. C-WP1 legacy-ds-token（Integration profile）

| 项 | 值 |
|----|---|
| Profile | `dev-local-multidb` |
| 配置 | `oa.auth.legacy-ds-token.enabled=true`（`application-dev-local-multidb.yml`） |
| 用途 | Integration 多库 + legacy token 解析；**非生产 cutover 目标** |
| 测试 | `MultidbDsSmokeIT` · `GatewayAuthProviderTest`（见会话 mvn 结果） |

---

**版本** v1.0 · **日期** 2026-07-29
