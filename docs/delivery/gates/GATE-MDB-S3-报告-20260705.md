# GATE-MDB-S3 报告 — 日志 + 消息 + 采集

> 日期：2026-07-05 · 阶段：S3 · 依据：`OPS-FOOTBALL-MULTI-DB-EXECUTION-PLAN.md` §S3

## 结论

**✅ 通过** — 登录/操作日志 Adapter 切 Football system DS；消息分场景文档化；采集 bind 字段；订单 `@DS("pay")`；IT 绿；E2E **58/58**。

## 实现摘要

| 项 | 状态 |
|----|------|
| S3-01 `LoginLogAdapter` → `system_login_log` @DS system | ✅ |
| S3-01 `OperateLogAdapter` → `system_operate_log` @DS system | ✅ |
| S3-02 消息分场景 | ✅ `sys_message` 留 wd（运营广播）；Football `system_notify_*` 不合并 |
| S3-03 采集 bind `mpAccountId` / `oaAccountId` | ✅ `CollectTaskRespVO` + 微信 mp 名称解析 |
| S3-04 订单只读 `@DS("pay")` | ✅ `FootballPayAllOrderReadMapper` |
| AC-S3-06 `sys_login_log` 停写 | ✅ `LogServiceImpl.recordLogin` no-op |
| Football 业务代码 diff | ✅ 无 system/pay-server 改动 |

## 字段映射（Adapter）

| Football | Ops VO |
|----------|--------|
| `result` 0/非0 | `status` SUCCESS/FAIL |
| `trace_id` / `log_type` | `message` |
| `user_ip` | `ip` |
| `type` / `sub_type` / `action` | `module` / `action` / `content` |
| `success` bit | `status` + `level` INFO/ERROR |

## 验收结果

| ID | 结果 | 证据 |
|----|------|------|
| AC-S3-UI-01 登录日志 ≥3000 | ✅ | IT `MdbS3LoginLogAdapterIT` |
| AC-S3-UI-02 操作日志 ≥600 | ✅ | IT `MdbS3OperateLogAdapterIT` |
| AC-S3-UI-03 采集 bind 字段 | ✅ | IT `MdbS3CollectTaskBindIT` · API `mpAccountId`/`oaAccountId` |
| AC-S3-UI-04 订单只读页 | ✅ | IT `MdbS3PayOrderReadIT` |
| AC-S3-UI-05 E2E 58/58 | ✅ | `run-uat-football-e2e.ps1` 2026-07-05 |
| AC-S3-AUX-01 football-order API | ✅ | `GET /admin-api/oa/football-order/list` |
| AC-S3-06 sys_login_log 停写 | ✅ | recordLogin no-op |

## 消息分场景（S3-02）

| 场景 | 数据源 | Ops 菜单 |
|------|--------|----------|
| 运营广播/渠道队列 | `wd.sys_message` | `#/ops/system-message` |
| Football 站内信/公告 | `system_notify_*` / `system_notice*` | Football 侧栏（只读，不合并） |

## 阻塞 / 后续

1. **AC-S3-UI 人工写操作** — E2E 为路由 smoke；`:5777` 登录/操作日志列表行数、采集页 bind 列需浏览器补签。
2. **S4** — DROP `oa_author` · cutover · 全场景复验。

## 启动命令

```powershell
.\scripts\start-integration-oa.ps1   # profiles 含 dev-local-multidb
.\scripts\run-uat-football-e2e.ps1
mvn -pl ops-platform-module-oa test -Dtest=MdbS3LoginLogAdapterIT,MdbS3OperateLogAdapterIT,MdbS3PayOrderReadIT,MdbS3CollectTaskBindIT
```
