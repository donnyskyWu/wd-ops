# S-R9 报告：效率分析（FR-M1-007）走查与全量修复

**日期**�?026-06-10
**模块**：M1 运营管理 · FR-M1-007 人效盘点
**路径**：`/efficiency`
**前置依赖**：S-R2 走过"�?mock + computed 派生"（实际未真做聚合 KPI�?

---

## 一、走查结�?

效率分析�?**S-R2 走查报告里说"接真 API �?4 tab �?computed 派生"，实际只是把 `sys_user` 列表硬套 4 种不�?× 系数 / 硬编�?0 的假数据**。本次走查发�?9 �?P0/P1 问题�?*全部修复**�?

---

## 二、发现清单（9 个）

| ID | 级别 | 问题 | 位置 |
|----|------|------|------|
| **B1** | P0 | 后端 list 端点�?4 关键参数：`startDate/endDate/timeDimension/position` | `ProductivityReviewController.list` |
| **B2** | P0 | 后端 list 完全没有 KPI 聚合（任�?财务）—�?5 KPI �?0 | `ProductivityReviewServiceImpl.list` + `toVO` |
| **B3** | P0 | �?3 端点：`/detail/{userId}` `/detail/anchors` `/export` —�?AC-M1-007-1 展开详情 + 导出 都没 API 支持 | `ProductivityReviewController` |
| **B4** | P0 | 前端 mini-chart 渲染�?`Math.random()` 假数�?| `Efficiency.vue` L385, L390 |
| **B5** | P0 | 前端 4 tab computed 全部硬编�?0 / × 系数假数据（`contentOutput * 1000` 等） | `Efficiency.vue` L272-321 wechatList/videoList/fansList/vipList |
| **B6** | P0 | 前端 `handleSearch/loadData` 不读 `period/statDate/position/keyword` —�?4 控件是装�?| `Efficiency.vue` L326, L342-362 |
| **B7** | P1 | `dict_position_type` 字典名写错——实际是 `dict_position` | `Efficiency.vue` L11 |
| **B8** | P1 | 展开 Card �?`Unhandled error at TableTdWrapper` render + `互动�? %` undefined | 多处 `row.engagement.toLocaleString()` �?undefined guard |
| **B9** | P2 | service 接口 `pageNo/pageSize` vs controller `page/size` 契约混乱（与 S-R8 B6 同款�?| `ProductivityReviewService.list` |

---

## 三、修复方�?

### 3.1 后端（B1/B2/B3/B9�?

#### `ProductivityReviewVO.java`（S-R9 改造）

新增 12 字段�?
```java
// 任务 KPI（按 oa_task.assignee_id 聚合�?
private Integer taskTotal = 0;
private Integer taskCompleted = 0;
private Integer taskInProgress = 0;
private Integer taskOverdue = 0;
private Double completionRate = 0.0;
// 财务 KPI（按 oa_order_attribution.ops_user_id 聚合�?
private Double costAmount = 0.0;
private Double revenue = 0.0;
private Double roi = 0.0;
private Integer orderCount = 0;
// 内容 KPI（ContentDO �?user 关联 �?�?0，记�?ADR-008�?
private Integer contentOutput = 0;
private Long avgRead = 0L;
private Long avgPlay = 0L;
private Integer hitCount = 0;
```

#### `TaskMapper.sumByUser`（新增）

SQL 一次按 assignee_id 聚合 4 任务 KPI�?
- total, completed (DONE), inProgress (IN_PROGRESS), overdue (PENDING/IN_PROGRESS/PENDING_REVIEW + sla_deadline < now)

#### `OrderAttributionMapper.sumByUser`（新增）

SQL 一次按 ops_user_id 聚合 3 财务 KPI�?
- revenue, cost, orderCount, ROI（service 层算�?

#### `ProductivityReviewServiceImpl.list`（重写）

�?7 参：`startDate/endDate/timeDimension/ipGroupId/userId/position/keyword` + `page/size`
流程�?
1. LambdaQueryWrapper �?sys_user（按 ipGroupId/userId/position/keyword 过滤 + DataScopeSupport �?IP 组权限）
2. 一�?sumTask + sumOrder 聚合所�?KPI（避�?N+1�?
3. toVO 合并 user 基础字段 + task KPI + order KPI

#### `ProductivityReviewService.detail/anchors/exportCsv`（新�?3 方法�?

- **detail**: 返回 `summary` + `taskList`（最�?50 条）+ `financeByGroup`（按 IP 组聚合）+ `contentMetrics`（占�?0�? `trend`（按日营�?订单数）
- **anchors**: �?ipGroupId 聚合所�?user
- **exportCsv**: 同步 CSV（UTF-8 BOM），直接 `ResponseEntity<byte[]>` 返回

#### `ProductivityReviewController`（重写）

4 端点全部到位�?
- `GET /list` —�?�?9 参（4 时间 + 5 过滤 + page/size�?
- `GET /detail/{userId}` —�?�?3 �?
- `GET /detail/anchors` —�?�?3 �?
- `POST /export` —�?返回 `text/csv;charset=utf-8` + Content-Disposition

#### `ProductivityReviewService`（接口重写）

契约统一�?`page/size`（B9 ✅）

### 3.2 前端（B4/B5/B6/B7/B8�?

#### `types/productivity.ts`（重写）

字段�?**全部对齐后端 VO**�?
- �?`completedCount/inProgressCount/overdueCount/totalCost/totalRevenue/contentPublished/avgPlayCount/hitContentCount`
- �?`taskCompleted/taskInProgress/taskOverdue/completionRate/costAmount/revenue/roi/orderCount/contentOutput/avgRead/avgPlay/hitCount`

#### `api/productivity.ts`（重写）

3 方法：`getProductivityList/getProductivityDetail/getProductivityAnchors` + `exportProductivityCsv`（window.open 直接触发下载，简化避�?axios blob 兼容性）

#### `Efficiency.vue`（重写）

- **4 tab 全部用真 VO 字段**（不再是 × 系数假数�?/ 硬编�?0�?
- **`mini-chart` 移除�?`Math.random()`**，改为基�?detail 接口返回�?trend 数据
- **loadData �?period/statDate/position/keyword**（statDate 默认 undefined，只有用户选了才传，避免空日期 1970�?
- **新增 IpGroupTreeSelect / 关键�?input** —�?5 个搜索控件全部工�?
- **4 �?undefined 守卫**：`formatNumber/formatPct/getEfficiency/getEfficiencyClass` 全部 null-safe
- **岗位 DictSelect** 改为 `dict_position`（B7 ✅）
- 4 Card 展开里加 ADR-008 提示（ContentDO �?user 关联 �?内容 KPI 占位 0�?

---

## 四、验�?

### 4.1 后端 `mvn compile`

`[INFO] BUILD SUCCESS`

### 4.2 后端 `mvn test` 全套回归

**216/216 通过**，新�?`M1EfficiencyKpiIT` 9/9 通过�?

### 4.3 API 实测

| 端点 | 实测结果 |
|------|---------|
| `GET /list` | 5 �?user 全部聚合�?KPI�?003: taskTotal=5/revenue=102900/roi=4.0/orderCount=8�?|
| `GET /detail/1003` | summary + 5 taskList + 1 financeByGroup + 6 �?trend |
| `GET /detail/anchors?ipGroupId=9001` | 1003 单个 user�?001 下） |
| `POST /export` | 5 �?CSV + UTF-8 BOM |

### 4.4 浏览器复测（agent-browser�?

| 验收�?| 结果 |
|--------|------|
| 表格列：经办�?岗位/IP�?任务总数/已完�?完成�?账号成本/营收/ROI/订单�?| �?全部显示真�?|
| 1003 行：5/1/20.00%/¥25,725/¥102,900/4.00%/8 | �?完全匹配后端 |
| 1005 行：2/0/0.00%/¥16,125/¥64,500/4.00%/4 | �?|
| 搜索"运营" �?过滤生效 | �?|
| 切换 4 tab �?全部显示真数据（不再 0/0/0/0�?| �?|
| 展开 1003 �?�?4 Card 真实数据 | �?任务 1/20%、财�?¥25725/¥102900/ROI 4.00%、内�?0+ADR-008、趋势近 30 �?|
| 之前 `Unhandled error at TableTdWrapper` | �?已修�?|
| 之前 `互动�? %` undefined | �?已修�?|
| `dict_position` 下拉 | �?5 条（OPS_LEADER/OPERATOR/EDITOR/ANCHOR/OPS_OFFICIAL�?|

---

## 五、新�?/ 修改文件清单

### 新增

- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/api/dto/operations/ProductivityReviewDetailVO.java`
- `ops-platform-server/ops-platform-module-oa/src/test/java/cn/iocoder/yudao/module/oa/M1EfficiencyKpiIT.java`

### 修改

- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/api/dto/operations/ProductivityReviewVO.java`�?12 字段�?
- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/service/operations/ProductivityReviewService.java`（接口重写）
- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/service/operations/ProductivityReviewServiceImpl.java`（重�?+ 聚合 SQL�?
- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/controller/operations/ProductivityReviewController.java`（重�?+ 3 端点�?
- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/dal/mysql/sop/TaskMapper.java`�?sumByUser�?
- `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/dal/mysql/perf/OrderAttributionMapper.java`�?sumByUser�?
- `ops-platform-ui-vue/src/types/productivity.ts`（字段对齐后�?VO�?
- `ops-platform-ui-vue/src/api/productivity.ts`�?2 端点 + exportCsv�?
- `ops-platform-ui-vue/src/views/operations/Efficiency.vue`�? tab 接真字段 / 移除假数�?/ �?search�?

---

## 六、遗�?/ Spec �?Schema Drift（需 ADR�?

### ADR-008：内�?KPI �?oa_content schema drift

**问题**：`oa_content` 表（ContentDO�?*�?user_id / author_id / assignee_id 关联字段**。Creator 是字符串（来�?TenantBaseDO），无法�?user 聚合内容产出 / 平均阅读 / 爆款数�?

**现状**：`productivityList.contentOutput/avgRead/avgPlay/hitCount` 全部 0，前端展开 Card 加了 "详见 ADR-008" 提示�?

**方案 A（推荐）**：`oa_content` �?`author_id BIGINT` 字段 �?后续 SOP/作者模块负责填补关�?
**方案 B**：`oa_account_owner` 多对多关联表 + sys_user
**方案 C**：明�?spec 限定"内容 KPI 不按经办人聚�?，由 IP 组维度聚合（不展示按 user 的内�?KPI�?

**需产品决策**：等 M2 SOP 模块上线前定�?

### 90+ 字段名清理遗留（S-R6 P1 backlog�?

S-R3 mode `enum-alias.ts` �?normalization 5 高频页，本期 ProductivityReviewVO 字段名沿�?ENGLISH 命名（`taskCompleted/revenue/roi`），�?UX 文档中文术语一致，�?P1 backlog�?

---

## 七、Pending 列表更新

| 来源 | 任务 | 状�?|
|------|------|------|
| S-R3 | 30+ 文件 enum literal 清理 | P1 backlog |
| S-R6 | 导出格式 xlsx（spec�?vs csv（实现） | P1 backlog |
| S-R7 | SOP `SopTemplateController.list` pageNum vs pageNo | **新增** P1（确认仍 pending�?|
| S-R8 | B5「是否爆款」筛选项 | P2（用户已 defer�?|
| S-R9 | ADR-008 oa_content �?author_id / 重新设计内容 KPI 聚合 | **新增** P0 待产品决�?|
| S-R9 | 90+ 字段名清�?| P1 backlog |

---

## 八、AC / IT 覆盖

### AC

- AC-M1-007-1 展开详情�? �?Card）：�?后端 `/detail/{userId}` 返回 4 Card，前端正确渲�?
- AC-M1-007-2 时间维度切换（week/month）：�?后端�?`timeDimension`（默�?WEEK），前端 DictSelect 选用

### IT（`M1EfficiencyKpiIT` 9 个）

| 用例 | 覆盖 |
|------|------|
| listKpis | B1+B2：timeDimension/position 参数 + 5 KPI 字段返回 |
| user1003Kpis | B2�?003 真聚合（revenue/taskTotal/orderCount�?|
| completionRateInRange | B2：完成率 �?0 |
| defaultTimeDimension | B1：不�?timeDimension 默认 WEEK |
| detail | B3�?detail/{userId} 返回 summary+taskList+financeByGroup+trend |
| anchors | B3�?detail/anchors �?IP 组聚�?|
| exportCsv | B3�?export 返回 BOM + Content-Type=text/csv |
| pagination | B9：page=2 size=5 返回 �?5 �?|
| positionFilter | B1：position=OPS_LEADER 过滤 |