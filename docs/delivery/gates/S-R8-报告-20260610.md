# S-R8 报告 · 作品分析（FR-M1-005）走�?

**日期**�?026-06-10
**走查页面**：`/works-analysis`（vue：`ops-platform-ui-vue/src/views/operations/WorksAnalysis.vue`�?
**对应后端**：`ContentAnalysisController` `/admin-api/oa/content-analysis/{list,stats,trend}`
**前置切片**：S-R2（VO 字段对齐 + `/trend` 端点）、S-R3（normalizePlatform + page/size）、P-GATE-UNMOCK（移�?list 假数据）

---

## 1. 走查发现�? �?P0/P1 + 1 �?P2 已修复，1 �?P2 推迟�?

| ID | 级别 | 问题 | 位置 |
|----|------|------|------|
| **B1** | P0 | 5 统计�?= 当前�?10 �?list.reduce 累加�?*与全�?26 条不符，分页切页会变**�?| `WorksAnalysis.vue` L133-142 `statFromList` |
| **B2** | P0 | 导出 columns 用错的字段名 `viewCount/shareCount`（后端实�?`readCount/forwardCount`）→ CSV 阅读�?转发列是�?| `WorksAnalysis.vue` L288-300 `handleExport` |
| **B3** | P0 | 详情弹窗永远空白。原因：(a) `getContentTrend(contentId)` 调错路径 `/content-analysis/${id}/trend`（后端实�?`/trend` query param�?03�?b) `ContentTrendPoint` 类型字段 `viewCount/interactionCount` 错（实返 `readCount/likeCount/commentCount/forwardCount`�?| `api/works.ts` L25 + `types/works.ts` L38-42 |
| **B4** | P1 | 详情应按 contentId 取该作品趋势。S-R2 阶段 `/trend` 设计时根本没考虑详情场景 | `ContentAnalysisService.trend` + `ContentAnalysisController.trend` |
| **B5** | P2 | 缺失「是否爆款」筛选项（UX-M1 §203 提了，spec §4.5.1 没强 AC�?| `WorksAnalysis.vue` TableSearch |
| **B6** | P2 | `ContentAnalysisService.list` 接口签名 `pageNo/pageSize`，controller �?`page/size`，契约混�?| `ContentAnalysisService.java` L13-15 |

---

## 2. 修复策略

### B1 · 5 统计卡后�?SQL 聚合（与 S-R6 粉丝同款�?

**根因**：`statFromList` 在前�?`list.reduce`�?*结果只反映当前页 10 �?*而非全量 26 条。S-R6 粉丝分析已踩过同款坑（`FollowerAnalysisController /stats` + `FollowerDailyMapper.sumStats`）�?

**修复**�?
- �?`ContentStatsVO` �?`totalLikes / totalComments / totalShares` 3 字段
- `ContentMapper.sumStats` 一次�?SQL 聚合 5 KPI
- `ContentAnalysisServiceImpl.stats` 改用 `contentMapper.sumStats(...)`，移�?in-memory `selectList + stream`
- 前端 `WorksAnalysis.vue` 移除 `statFromList`，`stats.value` 直接覆盖 5 字段

**代码**�?
```java
// ContentMapper.sumStats
SELECT
  COUNT(*) AS totalCount,
  SUM(CASE WHEN is_hit = 1 THEN 1 ELSE 0 END) AS hitCount,
  COALESCE(SUM(read_count), 0) AS totalRead,
  COALESCE(SUM(like_count), 0) AS totalLikes,
  COALESCE(SUM(comment_count), 0) AS totalComments,
  COALESCE(SUM(forward_count), 0) AS totalShares
FROM oa_content
WHERE tenant_id = #{tenantId} AND deleted = 0 AND account_id IN (...)
```

**dev 验证**（MySQL seed）：
- 修复前（前端聚合）：发布=26 / 阅读=1,185,500 / 点赞=31,720 / 评论=841 / 转发=3,991�?*�?3 字段是当前页 10 行聚�?*�?
- 修复后（后端聚合）：发布=26 / 阅读=1,185,500 / 点赞=46,920 / 评论=1,121 / 转发=4,191�?*全量 26 条聚�?*�?

### B2 · 导出 columns 字段名修�?

**根因**：`handleExport` �?`viewCount/shareCount`（S-R2 之前的字段名），S-R2 阶段对齐 VO �?*漏改导出 columns**�?

**修复**：columns �?`readCount / likeCount / commentCount / forwardCount` + �?`isHit`。CSV �?`exportToExcel` 生成�?UTF-8 BOM 字符串（S-R6 fan-analysis 同期已切 CSV，无 POI 依赖；spec xlsx 推迟�?M10+ easyexcel 引入后）�?

### B3 · 详情弹窗 trend 路径 + 字段双修

**根因**：S-R2 阶段 `getContentTrend` API 误把 contentId 放在 URL 路径 `/content-analysis/${id}/trend`，后端实际是 `/trend` query param，端点不存在 �?403 �?eCharts 无数�?�?弹窗�?+ 类型字段 `viewCount/interactionCount` 也不存在 �?渲染空�?

**修复**�?
- `api/works.ts` `getContentTrend(params: { contentId; startDate?; endDate? })`，URL �?`/oa/content-analysis/trend` + params query
- `types/works.ts` `ContentTrendPoint` 字段�?`count/readCount/likeCount/commentCount/forwardCount` 与后端实返一�?
- 前端 eCharts 配置 `legend.data` �?`['阅读�?, '互动�?]`（保持与 series name 一致，**避免 console warning**�?
- series 数据�?`d.viewCount` �?`d.readCount`（阅读量），互动�?= `likeCount + commentCount + forwardCount`（后端不直接�?interactionCount�?

### B4 · trend 端点�?contentId

**修复**�?
- `ContentAnalysisController.trend` �?`@RequestParam(required = false) Long contentId`
- `ContentAnalysisService.trend` 签名�?`Long contentId`
- `ContentAnalysisServiceImpl.buildContentWrapper` �?`contentId` 参数，trend 时调�?`buildContentWrapper(..., contentId)` �?`.eq(contentId != null, ContentDO::getId, contentId)`
- 前端 `handleViewDetail(row)` �?`renderTrendChart(row.id)` �?`getContentTrend({ contentId: row.id })`

**dev 验证**（MySQL seed）：
- `contentId=9304`（SEED-抖音作品D）→ `[{date:"2026-06-04", count:1, likeCount:4000, readCount:80000, forwardCount:800, commentCount:150}]` �?
- `contentId=9303`（SEED-公众号作品C 爆款）→ `[{date:"2026-06-03", count:1, likeCount:8000, readCount:150000, forwardCount:2000, commentCount:300}]` �?

### B6 · service 契约统一 page/size

**修复**：`ContentAnalysisService.list` 签名参数 `Integer pageNo, Integer pageSize` �?`Integer page, Integer size`，impl 同步改�?

---

## 3. 修复范围

| 文件 | 类型 |
|------|------|
| `ops-platform-server/ops-platform-module-oa/src/main/java/.../api/dto/operations/ContentStatsVO.java` | 改（+3 字段�?|
| `ops-platform-server/ops-platform-module-oa/src/main/java/.../dal/mysql/operations/ContentMapper.java` | 改（+sumStats 方法�?|
| `ops-platform-server/ops-platform-module-oa/src/main/java/.../service/operations/ContentAnalysisService.java` | 改（page/size + contentId�?|
| `ops-platform-server/ops-platform-module-oa/src/main/java/.../service/operations/ContentAnalysisServiceImpl.java` | 改（stats �?SQL + trend �?contentId + helper�?|
| `ops-platform-server/ops-platform-module-oa/src/main/java/.../controller/operations/ContentAnalysisController.java` | 改（trend �?contentId�?|
| `ops-platform-server/ops-platform-module-oa/src/test/java/.../M1WorksAnalysisStatsIT.java` | **新增** 7 �?IT |
| `ops-platform-ui-vue/src/api/works.ts` | 改（trend path + params�?|
| `ops-platform-ui-vue/src/types/works.ts` | 改（ContentStats + ContentTrendPoint + ContentAnalysisQuery�?|
| `ops-platform-ui-vue/src/views/operations/WorksAnalysis.vue` | 改（5 KPI / 导出 / 详情�?/ loadData�?|

---

## 4. 验证

### 4.1 后端 mvn test

- `mvn test -Dtest=M1WorksAnalysisStatsIT` �?**7/7 通过** �?
- `mvn test` 全套 �?**207/207 通过**（无回归；S-R8 新增 7 �?IT）✅
- `mvn test -Dtest=M1AnalysisS06IT`（ContentAnalysis baseline）→ 10/10 �?

### 4.2 后端 API curl（dev MySQL seed�?

```
GET /admin-api/oa/content-analysis/stats
�?{totalCount:26, hitCount:10, totalRead:1185500, avgRead:45596, totalLikes:46920, totalComments:1121, totalShares:4191}

GET /admin-api/oa/content-analysis/stats?ipGroupId=9001
�?{totalCount:14, hitCount:6, totalRead:671000, avgRead:47928, totalLikes:27850, totalComments:740, totalShares:3215}

GET /admin-api/oa/content-analysis/trend?contentId=9304
�?[{date:"2026-06-04", count:1, likeCount:4000, readCount:80000, forwardCount:800, commentCount:150}]

GET /admin-api/oa/content-analysis/trend?contentId=9303
�?[{date:"2026-06-03", count:1, likeCount:8000, readCount:150000, forwardCount:2000, commentCount:300}]
```

### 4.3 浏览器复测（agent-browser�?

| 检查项 | 修复�?| 修复�?|
|--------|--------|--------|
| 5 统计卡数�?| 31,720 / 841 / 3,991�?*当前页聚�?*）| 46,920 / 1,121 / 4,191�?*全量聚合**）|
| 切第二页 5 数字 | 变化（错误）| 保持 26/1,185,500/46,920/1,121/4,191（正确）|
| 详情弹窗 canvas | 0 个（空）| 1 个（eCharts 渲染）|
| 详情图数�?| N/A | 80000 阅读 / 4000+150+800 互动�?304 单日趋势）|
| 导出按钮 | �?导出成功"�?*�?CSV 阅读/转发列空**）| �?导出成功，共 10 条数�?�?*字段正确**）|
| Console warning | "互动数（点赞+评论+转发�? series not exists | 干净 |
| 4 筛选项 | 4 控件 | 4 控件（B5 是否爆款筛按用户决定不做）|

---

## 5. 遗留 / 推迟

| ID | �?| 原因 | 计划 |
|----|------|------|------|
| B5 | 「是否爆款」筛选项 | spec §4.5.1 唯一 AC 是爆款识别（红标签），UX-M1 §203 提了筛但 spec 没强 AC；本�?S-R8 用户决定不做 | 加进 S-R8+ 候选，由用户后续决�?|
| 切页 stats 验证 | 自动�?IT 验证"切第二页 5 数字不变" | 需要前�?playwright E2E | 加进 S-R8+ E2E 候�?|

---

## 6. 反�?

- **后端聚合先于前端**这条原则�?S-R6 已被确立为模式（S-R6-B1 粉丝 stats），S-R8 复用了同款策略�?*复盘说明：每次涉�?KPI 卡片 + 列表分页的页面，stats 端点必须先行后端聚合**�?
- S-R2 阶段对齐 VO 字段名时�?*只对齐了表格渲染，漏对齐导出 columns**（B2）。提示：字段名变更需要全局 grep `viewCount` `shareCount` 一次�?
- S-R2 阶段新增 `/trend` 端点时，**只考虑�?按筛选条件聚�?场景，没考虑"详情取单作品趋势"场景**（B3+B4）。提示：trend 端点签名要预�?contentId 过滤能力�?
- eCharts legend.data �?series name 不一致会触发 console warning —�?*前端 console hygiene**也属�?清理范围"�?
