# S-R12 报告：IP 组管�?走查

**日期**�?026-06-10
**模块**：M1 运营管理 / IP 组管�?
**路径**：`/admin-api/oa/ip-group/...` + `/ip-group`
**前置**：v1.0 S3 后端交付完成 + S-R3~R11 走查沉淀
**走查方式**：方法论 7 步（**S-R11 升级�?3 字段对照�?**—�?0 分钟内暴�?4 个真 bug�?

---

## 一、走查结�?

> **IP �?4 �?P0 + 1 �?P1 + 1 个后端缺失端�?*——`createTime/createdAt` 字段名错位、编辑回填字段缺失、`/list` 端点漏实现（`/list` �?`/{id}` �?500）�?*后端 service + controller 1 �?P0 缺失**—�? 步漏的端点实现�?

---

## 二、发现清�?

| ID | 级别 | 模块 | 现象 | 位置 | 验证 |
|----|------|------|------|------|------|
| **B1** | **P0** | 前端 type 错位 | `IpGroup.vue` �?`currentNode.createdAt` 渲染，后�?`IpGroupTreeVO.createTime` | `views/operations/IpGroup.vue:75,108` | 静态对�?|
| **B2** | **P0** | 前端 type 缺失 | `handleEdit` �?`currentNode.value.sortOrder` —�?TreeVO 无此字段 | `views/operations/IpGroup.vue:555` | 静态对�?|
| **B3** | **P0** | 前端 type 缺失 | `handleEdit` �?`currentNode.value.remark` —�?TreeVO 无此字段 | `views/operations/IpGroup.vue:557` | 静态对�?|
| **B4** | **P0** | **后端端点缺失** | `spec API-M1 §2.2` 定义 `GET /ip-group/list`，controller 漏实现——`GET /list` 路由�?`/{id}` �?"list" �?id，报 500 | `controller/ipgroup/IpGroupController.java:46` | IT-5 复现 500 |
| **B5** | P1 | 后端 service 缺失 | `IpGroupService` 接口�?`listPage` 方法 | `service/ipgroup/IpGroupService.java` | grep |
| **B6** | P2 | 后端 DTO 缺失 | `IpGroupListVO` 不存在（spec 要求�?| `api/dto/ipgroup/` | glob |
| **B7** | P1 | 前端 API 缺失 | `api/ip-group.ts` �?`getIpGroupDetail`（B2/B3 修复用） | `api/ip-group.ts` | 静态扫�?|
| **B8** | P2 | 字段错位 | `IpGroupDO.leaderUserId`（DB 字段），但前�?type 字段 `leaderId` | 后端 �?前端 | 静态对�?|

---

## 三、修复方�?

### 3.1 后端（B4/B5/B6�?
- �?�?`IpGroupListVO` DTO（`api/dto/ipgroup/IpGroupListVO.java`�?
- �?`IpGroupService` 接口�?`listPage(String groupName, Integer groupType, Integer status, Integer pageNum, Integer pageSize)`
- �?`IpGroupServiceImpl.listPage` 实现（按 `sortOrder, id desc` 排序，批量取 parentName/leaderName，count 字段初始 0�?
- �?`IpGroupController.list` 端点�?`@GetMapping("/list")`�?*必须排在 `/{id}` 之前**，否�?Spring 会把 "list" �?id 字符串匹配到 `/{id}`�?

### 3.2 前端（B1/B2/B3/B7�?
- �?`IpGroup.vue` �?75/108 �?`createdAt` �?`createTime`
- �?`IpGroup.vue` `handleEdit` 改异步：先调 `getIpGroupDetail` 拿全字段再回�?
- �?`api/ip-group.ts` �?`getIpGroupDetail(id: number)` 函数（走 `GET /ip-group/{id}`�?
- �?`types/ip-group.ts` �?`IpGroupDetailVO` 类型（对齐后�?`IpGroupDetailVO`�?

---

## 四、验�?

### 4.1 后端 mvn test 回归 �?

```
mvn test
[INFO] Tests run: 229, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

**7 个新 IT 全绿**�?
- �?`getDetailReturnsCreateTime` �?`GET /9000` �?`data.createTime` 字段
- �?`getDetailReturnsSortOrder` �?`data.sortOrder` 字段
- �?`getDetailReturnsRemark` �?`data.remark` 字段
- �?`getTreeReturnsNested` �?树形结构
- �?`listAcceptsPageNum` �?`GET /list?pageNum=1&pageSize=20` 200 + 数组
- �?`deleteEndpoint` �?`DELETE /delete?id=` 端点存在
- �?`statusToggle` �?`PUT /9000/status` �?status 1�? 真值切�?

### 4.2 浏览器复�?⏸️

**未做**（dev 进程未启）。预期实证：
- 树左侧加�?`>0` �?
- 节点点击后右�?header 显示 `createTime`（非 `-`�?
- 编辑对话�?sortOrder/remark 真值回�?
- 分页切到�?2 �?URL �?`pageNum=2`

---

## 五、修�?/ 新增文件

| 文件 | 变更 |
|------|------|
| `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/api/dto/ipgroup/IpGroupListVO.java` | **新增**：spec §2.2 DTO |
| `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/controller/ipgroup/IpGroupController.java` | 改：�?`@GetMapping("/list")` + import PageResult/IpGroupListVO |
| `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/service/ipgroup/IpGroupService.java` | 改：�?`listPage` 方法签名 |
| `ops-platform-server/ops-platform-module-oa/src/main/java/cn/iocoder/yudao/module/oa/service/ipgroup/IpGroupServiceImpl.java` | 改：实现 `listPage` |
| `ops-platform-server/ops-platform-module-oa/src/test/java/cn/iocoder/yudao/module/oa/M1IpGroupDetailIT.java` | **新增**�? �?IT |
| `ops-platform-ui-vue/src/api/ip-group.ts` | 改：�?`getIpGroupDetail` |
| `ops-platform-ui-vue/src/types/ip-group.ts` | 改：�?`IpGroupDetailVO` |
| `ops-platform-ui-vue/src/views/operations/IpGroup.vue` | 改：`createdAt` �?`createTime`、`handleEdit` 改异步调 `getIpGroupDetail` |

---

## 六、反�?

### 6.1 关键沉淀
1. **"3 字段对照�?再显威力**：S-R11 沉淀�?前端 import �?api export / api URL �?controller / controller @RequestParam �?前端 params"—�?*这次抓到�?controller 端点缺失 + service 缺失 + DTO 缺失** 3 个层级的问题。S-R11 错过 1 个：还要�?**"前端用到�?type 字段 �?后端 VO/DO 字段"** �?4 列对照�?
2. **`/list` vs `/{id}` 路由冲突**：Spring MVC 路径匹配**精确优先**——`@GetMapping("/list")` 必须排在 `@GetMapping("/{id}")` 之前才不会被 "list" 字符串当 id。这�?S-R3 教训（S-R3 修过 FollowerAnalysis）的同型 bug�?
3. **S-R11 教训升级�?v2**：报告自�?7 条之外，**还应�?verify "IT 失败信息 vs 实际数据真�?**——`NumberFormatException: "list"` 直接告诉你是路径被字符串匹配�?

### 6.2 留给下个 slice �?TODO
- **浏览器复�?*：dev 起来�?agent-browser 验证 4 期望实证
- **B8 (P2 leaderId �?leaderUserId)**：待 SOP 走查时统一收口

### 6.3 方法论升�?
- **"3 字段对照�? �?"4 字段对照�?**�?
  1. 前端 import �?api export
  2. api URL �?controller
  3. controller @RequestParam �?前端 params
  4. **前端 type 字段 �?后端 VO/DO 字段**（S-R12 新增�?

### 6.4 ADR 触发
- **ADR-M1-001-FieldAlias**（建议新增）：DB `leader_user_id` �?前端 `leaderId` 命名漂移问题
  - �?A：前端改 `leaderUserId`（跟 DB�?
  - �?B：后�?VO 注解 `@JSONField(name="leaderId")`（改映射不动字段�?
  - �?C：GATE-S7 已上线无影响，留 backlog
  - **推荐 A**（最小改动）

---

**报告完成时间**�?026-06-10 16:30
**作�?*：Cursor Agent + Donny
**状�?*：S-R12 修复完成 · 浏览器复测待下次�?dev
