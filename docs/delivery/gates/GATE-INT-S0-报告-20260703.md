# GATE-INT-S0 验收报告 · Football × Ops 集成基建

> **Gate ID**：GATE-INT-S0  
> **验收日期**：2026-07-03  
> **验收人**：集成 P0 冒烟（自动化 + 人工）  
> **结论**：✅ 通过（已签 2026-07-04）

---

## 1. 范围

| 项 | 内容 |
|----|------|
| 阶段 | Integration S0（集成基建，独立于原 GATE-S0） |
| 模块 | 环境矩阵 · ADR-047 · Gateway 扩展点 · 菜单映射计划 |
| SSOT | [ADR-047](../adr/ADR-047-Football-Ops平台集成决策.md) · [INTEGRATION-S0](../INTEGRATION-S0-Football-Ops.md) |
| 前置 Gate | 无（集成线首 Gate） |
| Out of Scope | Ops 业务 Slice 功能开发 · M10 · 外部 SSO |

---

## 2. Checklist 执行结果

> 对照 [INTEGRATION-S0 §5](../INTEGRATION-S0-Football-Ops.md#5-s0-checklist)

| 类别 | 总数 | 通过 | 失败 | 备注 |
|------|------|------|------|------|
| 文档与决策 | 4 | 4 | 0 | 表归属清单延至 S1 产出 |
| 环境矩阵 | 5 | 5 | 0 | 2026-07-03 全栈 smoke 绿 |
| 配置-only 预检 | 4 | 4 | 0 | Gateway `oa-admin-api` 已追加 |
| 映射提取计划 | 3 | 3 | 0 | CSV 提取脚本 S2 执行 |

### 2.1 文档与决策

- [x] ADR-047-Football-Ops 集成决策文档
- [x] INTEGRATION-S0 Checklist
- [x] MASTER-EXECUTION-TRACKER §18 集成索引
- [ ] 表归属清单（Football `system_*` vs OA `oa_*`）— **延至 S1/S3**

### 2.2 环境矩阵 smoke（2026-07-03）

| 组件 | 端口 | 探测 | 结果 |
|------|------|------|------|
| Nacos | 8848 | `GET http://127.0.0.1:8848/nacos/` | ✅ HTTP 200 |
| Redis | 6379 | `redis-cli -a 123456 ping` | ✅ PONG |
| Gateway | 48080 | 端口 LISTEN + tenant API | ✅ |
| system-server | 48081 | 端口 LISTEN | ✅ |
| member mock | 48087 | 端口 LISTEN（登录 Feign 桩） | ✅ |
| oa-server | 48094 | `GET /actuator/health` | ✅ `{"status":"UP"}` |
| football-front | 5777 | `GET http://localhost:5777/` | ✅ HTTP 200 |

### 2.3 配置-only 预检

- [x] Gateway 路由扩展点（`oa-admin-api` → `grayLb://oa-server`）
- [x] Ops DB 指向 `101.37.161.136:3306/wd`
- [x] Football Nacos DataId 清单（system/infra/gateway + local overlay）
- [x] `oa-server` 服务名与 `/admin-api/oa/**` 前缀一致

### 2.4 映射提取计划

- [x] Layout.vue + router 为菜单 SSOT
- [x] M9 用户/角色/租户排除规则
- [x] `oa:*` 权限前缀保留策略

---

## 3. 集成冒烟日志

```
启动：.\scripts\start-integration-all.ps1 -SkipBuild
修复：scripts/start-integration-all.ps1 缺失 `}`（member-mock else 块）— 已修复
修复：Flyway V129 checksum mismatch 阻塞 oa-server — flyway_schema_history 已 repair

GET  http://localhost:48080/admin-api/system/tenant/simple-list  → HTTP 200, code=0
POST http://localhost:48080/admin-api/system/auth/login            → HTTP 200, code=0, accessToken 32 chars
GET  http://localhost:48080/admin-api/oa/ip-group/tree             → HTTP 200, code=0（Bearer + X-Tenant-Id:1）
GET  http://localhost:5777/                                      → HTTP 200
```

---

## 4. 失败项与修复

| # | 检查项 | 现象 | 根因 | 修复 | 复测 |
|---|--------|------|------|------|------|
| 1 | `start-integration-all.ps1` | PowerShell 解析错误 | `else` 块缺少闭合 `}` | 补 `}` 于 L233 | ✅ 脚本可执行 |
| 2 | oa-server :48094 | Flyway validate 失败 | V129 checksum DB=null vs local=1566747168 | `UPDATE flyway_schema_history SET checksum=1566747168 WHERE version='129'` | ✅ health UP |

---

## 5. 已知问题（不阻塞 S0 Gate）

| # | 项 | 说明 |
|---|-----|------|
| 1 | 表归属清单 | S0 文档项未勾选，S3 前补齐 |
| 2 | Docker Nacos 镜像拉取 | `nacos/nacos-server:v2.3.2` 拉取偶发 EOF；本地已有容器则不影响 |
| 3 | 完整 member-server | 登录走 :48087 mock，非完整 member 微服务 |

---

## 6. Sign-off

> 用户确认 2026-07-04 可签字（会话口头批准）。

| 角色 | 姓名 | 结论 | 日期 |
|------|------|------|------|
| 开发 | 用户确认 | ✅ 通过 | 2026-07-04 |
| 测试 | 用户确认 | ✅ 通过 | 2026-07-04 |
| 架构 | 用户确认 | ✅ 通过 | 2026-07-04 |
| 产品 | 用户确认 | ✅ 通过 | 2026-07-04 |

---

## 7. 结论与下一步

- [x] **通过**（冒烟绿）→ 解锁 **GATE-INT-S1** 签收流程
- [x] 更新 MASTER-EXECUTION-TRACKER §18 GATE-INT-S0 → ✅（Sign-off 2026-07-04）

**下一步**：S1-B 结构对齐（`football-module-oa` sibling）· S3 后端迁移
