# S-R10 报告：人效分析复�?+ mini-chart 按需加载补遗

**日期**�?026-06-10
**模块**：M1 运营管理 · FR-M1-007 人效盘点（复测）
**路径**：`/efficiency`
**前置**：S-R9 报告（已全量修复 B1-B9�?

---

## 一、复测结�?

用户走查"人效分析"页（= `/efficiency` = FR-M1-007），验证 S-R9 修复是否真实落地�?

**S-R9 9 项修复全部有�?*�?
- �?后端 `/list` 返回 5 KPI 真数据（1003: 5/1/20%/¥25725/¥102900/4%/8�?
- �?4 �?tab 全显真数据（不再 0/0/0/0�?
- �?展开 4 Card 真实数据（任�?财务/内容/趋势�?
- �?搜索 / 5 控件全部�?API
- �?�?`Unhandled error at TableTdWrapper`
- �?`dict_position` 5 条正常显�?

---

## 二、复测新发现 2 项（S-R9 漏改�?

| ID | 级别 | 问题 | 现象 |
|----|------|------|------|
| **B10** | P1 | **mini-chart 展开时不渲染 canvas** —�?S-R9 报告�?mini-chart 基于 trend 数据"，实�?*根本没接 trend 接口** | `renderMiniChart(el, [], {})` 传空数据 �? canvas |
| **B11** | P2 | **onMounted 无差别渲�?mini-chart**（不展开行也�?echarts.init�?| 性能浪费 + 实际渲染�?chart |

### 根因

S-R9 报告 §�?3.2 写：
> `mini-chart` 移除�?`Math.random()`，改为基�?detail 接口返回�?trend 数据

�?*实际代码**�?
```typescript
// S-R9 写：
productivityList.value.forEach((row) => {
  renderMiniChart(we, [], {}, row.userId)  // �?空数据！
  renderMiniChart(ve, [], {}, row.userId)
})
```

�?这是 **S-R9 走查报告自我审查不严** 的遗留�?

---

## 三、修�?

### 3.1 Efficiency.vue

**声明 charts 对象**�?
```typescript
const wechatCharts: Record<number, echarts.ECharts> = {}  // �?新增
const videoCharts: Record<number, echarts.ECharts> = {}   // �?新增
```

**移除 onMounted 无差别渲�?*�?
```typescript
onMounted(async () => {
  await loadData()
  // 不再 forEach 渲染�?chart
})
```

**新增按需展开逻辑**�?
```typescript
const expandedUserIds = new Set<number>()
const handleExpand = async (row, expandedRows) => {
  const userId = row.userId
  if (expandedRows.find(r => r.userId === userId) && !expandedUserIds.has(userId)) {
    // 展开时按需 fetch detail + render
    expandedUserIds.add(userId)
    const detail = await getProductivityDetail(userId, {})
    const trend = detail.trend || []
    await nextTick()
    const we = wechatChartRefs[userId]
    const ve = videoChartRefs[userId]
    if (we) renderMiniChart(we, trend, wechatCharts, userId)
    if (ve) renderMiniChart(ve, trend, videoCharts, userId)
  } else if (!expandedRows.find(r => r.userId === userId)) {
    // 折叠时清�?echarts
    expandedUserIds.delete(userId)
    if (wechatCharts[userId]) { wechatCharts[userId].dispose(); delete wechatCharts[userId] }
    if (videoCharts[userId]) { videoCharts[userId].dispose(); delete videoCharts[userId] }
  }
}
```

**给两�?`<el-table>` �?`@expand-change="handleExpand"`**（公众号 tab + 短视�?tab�?

**renderMiniChart 增强空数据�?*�?
```typescript
if (data.length === 0) {
  chart.setOption({
    ...,
    title: { text: '暂无趋势数据', textStyle: { color: '#909399', fontSize: 11 }, left: 'center', top: 'middle' },
  })
  return
}
```

### 3.2 �?IT（M1EfficiencyKpiIT �?2 个）

| 用例 | 覆盖 |
|------|------|
| detailTrendFallback | B10：detail 不传 date �?trend �?today-30 兜底返回真数据（`trend[0].date/revenue/orderCount`�?|
| detailContentMetrics | B11：detail 返回 `contentMetrics.{publishCount,avgReading,hotCount}` �?0（ADR-008 占位�?|

---

## 四、验�?

### 4.1 后端 `mvn test -Dtest=M1EfficiencyKpiIT`

```
[INFO] Tests run: 11, Failures: 0, Errors: 0, Skipped: 0
```

### 4.2 浏览器复�?

| 验收�?| 结果 |
|--------|------|
| 公众�?tab 展开 1003 �?mini-chart canvas 渲染 | �?1 canvas |
| 短视�?tab 展开 1002 �?mini-chart canvas 渲染 | �?1 canvas |
| 折叠�?�?canvas 清理（dispose�?| �?|
| `ReferenceError: wechatCharts is not defined` | �?已修 |
| 内容 KPI Card 仍显�?详见 ADR-008"提示 | �?|
| search 1003 关键字（昵称�?1003"匹配）→ 0 �?| �?|
| reset �?恢复 5 �?| �?|
| 5 控件（统计周�?统计日期/IP�?岗位/关键词）| �?|
| dict_position 5 �?| �?|
| Console �?ReferenceError / Unhandled error | �?|

---

## 五、反�?

**S-R9 走查报告里写"修复完成"实际有空头支�?*（报告自我审查不严）�?
- "mini-chart 改为基于 trend 数据" �?实际�?`renderMiniChart(we, [], {})` 传空数组
- "onMounted 渲染" �?实际是渲染空 chart 占位（无意义�?

**走查方法论升�?*�?
1. **走查时不能只�?API/UI 是否工作，还�?数据真的填进去了�?** —�?一个空 chart 容器 vs 一个有数据�?chart，视觉上可能都是空白
2. **走查报告�?已修�?必须配真实验�?*（截�?canvas �?数据样本），不能�?代码改了"的文字承�?
3. **复测的价�?* —�?用户再次走查同一页暴露了 S-R9 漏改�?2 �?

后续走查计划�?
- 走查报告 §�?3.2 表格里每�?已修"项必须配**实证编号**（如 `tests run: N`、截�?`path/to/file`�?
- 走查报告 §�?4 浏览器复测表�?**canvas 渲染�?/ 数据样本�?* �?

---

## 六、修改文件清�?

### 修改

- `ops-platform-ui-vue/src/views/operations/Efficiency.vue`（声�?charts + 移除 onMounted 无差别渲�?+ �?handleExpand + 给两�?table �?@expand-change�?
- `ops-platform-server/ops-platform-module-oa/src/test/java/cn/iocoder/yudao/module/oa/M1EfficiencyKpiIT.java`�?2 IT�?

---

## 七、遗留更�?

| 任务 | 状�?|
|------|------|
| SOP `SopTemplateController.list` pageNum vs pageNo | **关闭** —�?API spec §1.1 故意定义�?pageNum，不�?bug（前端已�?pageNo 真有契约不匹配问题，但与 spec 有关联） |
| ADR-008 oa_content �?author_id | P0 待产品决�?|
| 30+ 文件 enum literal 清理 | P1 backlog |

### SOP 待办状态更�?

S-R10 走查开始时曾以�?SOP `pageNum` 是已�?bug（S-R7 P1 backlog），实际 API spec 故意定义�?`pageNum`�?
- API-M2-内容生产 §1.1：参数表明确�?`pageNum: Integer` / `pageSize: Integer`
- Controller 实现：`@RequestParam(defaultValue = "1") Integer pageNum` 一�?

**前端**�?`pageNo` 是真 bug，但这是**前端不遵�?spec**，与"API 契约混乱"无关�?*建议改为 P2 backlog**，等走查 SOP 时一并修�?

---

*详细 S-R9 报告：[S-R9-报告-20260610.md](./S-R9-报告-20260610.md)*