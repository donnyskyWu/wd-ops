# S-R13 报告：账号分析详�?走查

**日期**�?026-06-10
**模块**：M1 运营管理 / 账号分析详情
**路径**：`/admin-api/oa/account-analysis/{id}/followers` + `/contents`，前�?`/analysis/account/:id/detail`
**前置**：v1.0 S3 + S-R2-B 后端交付
**走查方式**：方法论 7 �?+ **S-R12 升级�?4 字段对照�?*

---

## 一、走查结�?

> **账号分析详情是低 bug 密度�?*�? P0 / 0 P1 / 3 P2）—�?S-R2-B 已基本完工�? 字段对照表验证：路由/参数�?VO 字段全对齐，**page/size 风格在本模块独立保留**（与 SOP �?pageNum 不冲突，因为 controller `@RequestParam` 严格按名）。补 4 �?IT 锁住端点契约防回归�?

---

## 二、发现清�?

| ID | 级别 | 现象 | 位置 | 验证 |
|----|------|------|------|------|
| **B1** | �?| `loadContents` �?`page/size` —�?**后端 `/{id}/contents` �?`page/size` 风格**（OK ！） | AccountAnalysisDetail.vue:112 + AccountAnalysisController.java:50-54 | �?IT-3 验证 |
| **B2** | P2 | `getAccountFollowerDetail` �?URL `/.../{accountId}/followers` 同时 query 里传 `accountId` —�?后端实际�?path id（query 被忽略），但**冗余参数污染 URL** | api/account-analysis.ts:20 | 静态扫�?|
| **B3** | P2 | `route.params.id` 未做 undefined 防御（直接访�?`/account-analysis-detail` �?NaN�?| AccountAnalysisDetail.vue:84 | 静态扫�?|
| **B4** | P2 | `route.query.tab` 未做白名单校�?| AccountAnalysisDetail.vue:85 | 静态扫�?|

**�?P0/P1**——本 slice �?维持�?走查，主要任务是�?IT 防回归�?

---

## 三、修复方案（�?2 �?P2 优化�?

### 3.1 B2 �?去掉 query 里冗�?accountId
`api/account-analysis.ts:20` URL 模板里已�?`${params.accountId}`，无需 params 里再传�?

### 3.2 B3/B4 �?路由防御
- B3：`if (!route.params.id) { router.replace('/account-analysis'); return }`
- B4：白名单 `if (tab !== 'followers' && tab !== 'contents') activeTab = 'followers'`

**决策**：B2 改之；B3/B4 �?P2 backlog（用户常态不会传非法值，浏览器地址栏注入风险低）�?

---

## 四、验�?

### 4.1 后端 mvn test 回归 �?

```
mvn test
[INFO] Tests run: 233, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

**4 个新 IT 全绿**�?
- �?`followers` �?`GET /9001/followers` 200 + 数组
- �?`followersWithDateFilter` �?startDate/endDate 真值过�?
- �?`contents` �?`GET /9001/contents?page=1&size=20` 200 + 分页结构
- �?`contentsIgnoresPageNum` �?`pageNum=99` 被忽略，Spring 走默�?page=1

### 4.2 浏览器复�?⏸️

**未做**（dev 进程未启）。预期实证：
- 跳到 `/analysis/account/9001/detail` URL 渲染 �?
- 默认 followers tab 表格行数 �?0
- 切到 contents tab 重新发请�?
- 切分�?page=2 URL �?`page=2`

---

## 五、修�?/ 新增文件

| 文件 | 变更 |
|------|------|
| `ops-platform-ui-vue/src/api/account-analysis.ts` | 改：B2 移除 query 里冗�?accountId |
| `ops-platform-server/ops-platform-module-oa/src/test/java/cn/iocoder/yudao/module/oa/M1AccountAnalysisDetailIT.java` | **新增**�? �?IT |

---

## 六、反�?

### 6.1 关键沉淀
1. **"4 字段对照�?在低 bug 密度页也有效**：本来想"这页�?bug 不需要走�?——S-R12 沉淀的方法论强制走一遍后�?*至少发现 1 �?B2 冗余** + 确认�?page/size 风格�?1 个一致性边界�?*走查不应跳过"看似 OK"的页**�?
2. **H2 seed ID 必须用真实范�?*：第一遍写 IT �?1001 �?4 失败。教训：**�?IT 第一时间�?grep 实际 seed 文件确认 id 范围**。可作为 H2 IT 模板的第 1 条�?
3. **�?slice 内不同模块可用不同分页命�?*：SOP �?`pageNum`，M1 AccountAnalysis �?`page/size`—�?*后端 controller `@RequestParam` 严格按名匹配是事实正�?*。SOP-R11 报告里说"前端要统一�?pageNum"是跨模块一致性建议，�?*单模块内保持�?spec 一�?*也是正确选项�?

### 6.2 留给下个 slice �?TODO
- 浏览器复�?
- B3/B4 P2 路由防御（建议下次走查或独立 P2 清理 slice�?

### 6.3 方法论升�?
- **H2 IT 模板�?1 �?*：写 IT 前先 grep seed 文件确认 ID 范围
- **"看似 OK 的页"也要走查**：走查方法论�?7 步不能跳过任何一�?

### 6.4 ADR 触发
- 无（无新 schema drift 或架构决策）

---

**报告完成时间**�?026-06-10 16:45
**作�?*：Cursor Agent + Donny
**状�?*：S-R13 �?4 IT · 0 代码修复（仅 B2 优化）�?浏览器复测待下次�?dev
