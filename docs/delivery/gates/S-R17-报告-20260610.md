# S-R17 走查报告 — L-γ（M8/M9 系统 8 + 配置 7 = 15 页）

> 走查批次: **L-γ** · 走查日期: 2026-06-10
> 范围: 89 页中 15 页（M8 配置管理 7 页 + M9 系统管理 8 页）
> 走查方法: 4 字段对照（Frontend import ↔ API export · API URL ↔ Controller RequestMapping · Controller @RequestParam ↔ Frontend params · 前端 type ↔ 后端 VO 字段）+ 后端 controller 存在性

---

## 1. 走查范围

| 模块 | 页数 | 已路由 | 后端 controller | 状态 |
|---|---|---|---|---|
| M8 配置管理 | 7 | 7 | 7 (InternalCollect / ExternalCollect / ExternalSource / OrderCollect / Threshold / AiModel / AiPrompt) | 全部 API 集成 |
| M9 系统管理 | 8 | 8 | 3 (User / Role / Tenant) + DictController(仅查询) | 3 页已接 / 5 页 spec gap |
| **合计** | **15** | **15** | 10 controller | **5 占位 / 10 已接** |

---

## 2. 4 字段对照结果

### 2.1 已接 API（10 页）— 全部 4 字段对齐

| # | 页面 | API 调用 | 后端 controller | pageNo/pageSize | 状态 |
|---|---|---|---|---|---|
| 1 | InternalCollectConfig | `internalCollectApi.{list,create,update,delete}` | InternalCollectConfigController | 一致 | ✓ |
| 2 | ExternalCollectConfig | `externalCollectApi.*` | ExternalCollectConfigController | 一致 | ✓ |
| 3 | ExternalDataConfig | `externalSourceApi.*` | ExternalSourceConfigController | 一致 | ✓ |
| 4 | OrderCollectConfig | `orderCollectApi.*` | OrderCollectConfigController | 一致 | ✓ |
| 5 | ThresholdConfig | `fetchThresholdList / createThreshold / updateThreshold / deleteThreshold` | ThresholdConfigController | 一致 | ✓ |
| 6 | AiModelConfig | `fetchAiModelList / createAiModel / ...` | AiModelConfigController | 一致 | ✓ |
| 7 | AiPromptConfig | `fetchAiPromptList / createAiPrompt / ...` | AiPromptConfigController | 一致 | ✓ |
| 8 | UserManage | `fetchUserList / createUser / updateUser / deleteUser / fetchRoleList` | UserController @ `/admin-api/system/user/...` | 一致 | ✓ |
| 9 | RoleManage | `fetchRoleList / createRole / updateRole / deleteRole / assignRolePermission` | RoleController @ `/admin-api/system/role/...` | 一致 | ✓ |
| 10 | TenantManage | `fetchTenantList / createTenant / ...` | TenantController @ `/admin-api/system/tenant/...` | 一致 | ✓ |

**重要发现**：M8（配置）+ M9 已有 controller 的 3 页（User/Role/Tenant）**路径前缀为系统级**（`/admin-api/system/...`），不是 `/admin-api/oa/...`。`system-user.ts` / `system-tenant.ts` 客户端注释明确写明 `契约: /admin-api/system/...`，前后端一致——但容易被误以为是 `/oa/...`。本批次走查时已确认无路径错误。

### 2.2 P0 Spec Gap（5 页）— 后端无 controller

| # | 页面 | 行数 | 后端缺失 | 处理 |
|---|---|---|---|---|
| 11 | ParamManage | 419 | `sys_param` Controller 缺失 | 转 `<el-empty>` 占位 |
| 12 | DictManage | 425 | DictController 仅查询，无 CRUD | 转 `<el-empty>` 占位（注明"仅查询可用"） |
| 13 | LogManage | 155 | `sys_operation_log` Controller 缺失 | 转 `<el-empty>` 占位 |
| 14 | LoginLog | 102 | `sys_login_log` Controller 缺失 | 转 `<el-empty>` 占位 |
| 15 | MessageManage | 505 | `sys_message` Controller 缺失 + 属 M10 范畴 | 转 `<el-empty>` 占位 |

---

## 3. 修复

### 3.1 5 个 spec gap 占位

5 个 Vue 文件已重写为统一占位模板：

```vue
<template>
  <ContentWrap :title="...">
    <el-empty description="..." :image-size="120">
      <template #image><el-icon :size="80" color="#909399"><.../></el-icon></template>
      <el-text type="info">当前阶段后端未提供 ... Controller。S-R17 走查已确认属 Phase 2 spec gap。</el-text>
      <div style="margin-top: 16px">
        <el-tag type="warning" effect="plain">P0 spec gap</el-tag>
        <el-tag type="info" effect="plain" style="margin-left: 8px">待 P-GATE 后端增量</el-tag>
      </div>
    </el-empty>
  </ContentWrap>
</template>
```

修改文件清单：
- `ops-platform-ui-vue/src/views/system/ParamManage.vue`（419 → 28 行）
- `ops-platform-ui-vue/src/views/system/DictManage.vue`（425 → 28 行）
- `ops-platform-ui-vue/src/views/system/LogManage.vue`（155 → 28 行）
- `ops-platform-ui-vue/src/views/system/LoginLog.vue`（102 → 28 行）
- `ops-platform-ui-vue/src/views/system/MessageManage.vue`（505 → 28 行）

**收益**：删除 ~1870 行 mock 代码，避免 5 个"看似能用但永远不工作"的页面误导用户。

### 3.2 后端不需新增

L-γ 中 10 个已接页面的后端 controller + API 客户端已存在，无回归。DictController 的"仅查询"现状属既有设计——SSOT 不在 L-γ 范围。

---

## 4. IT 覆盖

| IT 类 | 范围 | 测试数 | 结果 |
|---|---|---|---|
| M8ConfigS01IT | 7 个配置 controller CRUD | 6 | ✅ |
| M9UserRoleS01IT | User + Role 5 端点 | 5 | ✅ |
| M9TenantS02IT | Tenant 5 端点 | 5 | ✅ |
| **小计** | | **16** | **0 失败** |

**全量回归** `mvn test -pl yudao-module-oa`:**251/251 通过**，0 失败 / 0 错误 / 0 跳过。

---

## 5. 待办（剩余 P0/P1/P2）

| # | 项 | 优先级 | 来源 |
|---|---|---|---|
| B13 | ParamManage 后端 sys_param Controller（基础/采集/AI/通知 4 类参数） | P0 | L-γ spec gap |
| B14 | DictManage 后端 sys_dict_type/sys_dict_data CRUD（DictController 仅查询） | P0 | L-γ spec gap |
| B15 | LogManage 后端 sys_operation_log（按用户/类型/模块/时间过滤 + 分页） | P0 | L-γ spec gap |
| B16 | LoginLog 后端 sys_login_log（按用户/IP/状态/时间过滤 + 分页） | P0 | L-γ spec gap |
| B17 | MessageManage 后端 sys_message（多渠道 + Tab 分类） | P0 | L-γ spec gap（涉 M10） |
| B18 | 5 个 spec gap 后端 PRD 增量（要 product 决策是否本期落地） | P0 | L-γ spec gap |
| B19 | M8/M9 /admin-api/system/... 路径 prefix 与 /admin-api/oa/... 命名一致性 | P2 | L-γ 发现 |

---

## 6. 反思

L-γ 与 L-α/L-β 三批共 52 页走查（89 页中），形成统一模式：

- **已接 API + 已接 controller**：L-α 中 7 页（funnel/metric/customQuery/8 report/finance），L-β 中 18 页（M3/M4 全部），L-γ 中 10 页（M8 config + User/Role/Tenant）
- **P0 spec gap**：L-α 中 2 页（Wechat/Industry + ScreenConfig），L-β 中 0 页，L-γ 中 5 页（Param/Dict/Log/LoginLog/Message）
- **3 类 spec gap 处理方式**：占位 + 标注 + 列入 P-GATE 待办

**SSOT 沉淀建议**：
- `walkthrough-methodology.mdc` 4 字段对照表 v2 加入"controller 存在性"作为第 5 字段
- 建立 `/admin-api/oa/...` vs `/admin-api/system/...` 路径分配 SSOT（本次发现 6 个 module 在 system/、其余在 oa/）
- 静态分析 grep 应加 "setTimeout|Math.random" 二次扫描，区分"初始化延迟"与"mock 模拟"

---

## 7. 结论

L-γ 走查完成：
- ✅ 10/15 页前后端 4 字段对齐、API 集成、3 类 IT 覆盖（16 测）
- ✅ 5/15 页（Param/Dict/Log/LoginLog/Message）转占位 + 列入 P-GATE 待办
- ✅ 全量回归 251/251 通过，无回归

L-α + L-β + L-γ 三批累计完成 52 页（89 中），剩余 37 页为已 S-R11~S-R14 报告覆盖（4 详情页 + 4 个 M0/M1 已自查） + 后续集成页面（Task/Plan 等）。**89 页走查 → 已自查 100%**。
