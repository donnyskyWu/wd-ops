# ???? (DEPLOY.md)

> ???S5 / v9.1
> ??????+ ??????
> ???Spring Boot 3.x + Vue 3 + MySQL 8

---

## ??????

```
????????????????????   ?????????????????????
?? Vue 3 Frontend ?????? Spring Boot API  ??
?? (port 80)      ??   ??(port 8080)      ??
????????????????????   ?????????????????????
                                ??
                       ????????????????????
                       ??  MySQL 8       ??
                       ??  (port 3306)   ??
                       ????????????????????
```

> ???????? Redis / RabbitMQ / MinIO / XXL-JOB????ADR-001???
> ?? + ???? + ?????????? MVP ????????

---

## ???????

| ?? | ?? | ?? |
|------|------|------|
| JDK | 17+ | Spring Boot 3.x ?? |
| Maven | 3.8+ | ???? |
| Node.js | 18+ | ???? |
| MySQL | 8.0+ | utf8mb4 ????|
| ?? | ??GB | ????|
| ?? | ??0GB | ????|

---

## ?????

### 3.1 ??
```bash
cd ops-platform-server/ops-platform-module-oa
mvn clean package -DskipTests
# ???ops-platform-module-oa/target/ops-platform-module-oa-1.0.0.jar
```

### 3.2 ??????
```bash
# 1. ??????
mysql -uroot -p -e "CREATE DATABASE cursor DEFAULT CHARSET utf8mb4;"

# 2. ?? Flyway ??????????????
# ???V1.0.0 ??V1.0.8

# 3. ?????S3 ????
mysql -uroot -p cursor < db/V1.0.3__init_default_tenant_user.sql
```

### 3.3 ??
```bash
# ?? application.yml ??datasource / token-secret
java -jar ops-platform-module-oa-1.0.0.jar \
  --spring.datasource.url=jdbc:mysql://localhost:3306/cursor \
  --spring.datasource.username=root \
  --spring.datasource.password=root \
  --oa.auth.token-secret=YOUR_RANDOM_SECRET
```

### 3.4 ?????
```bash
curl http://localhost:8080/actuator/health
# ???{"status":"UP"}
```

---

## ?????

### 4.1 ??
```bash
cd yudao-ui-admin-vue3
npm install
npm run build:prod
# ???dist/
```

### 4.2 ???Nginx ????
```nginx
server {
  listen 80;
  server_name oa.example.com;
  root /var/www/oa/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /oa-api/ {
    proxy_pass http://127.0.0.1:8080/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
```

---

## ?????

### 5.1 `application.yml` ????
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cursor?useUnicode=true&characterEncoding=utf8
    username: root
    password: root
  flyway:
    enabled: true
    locations: classpath:db/migration

oa:
  auth:
    # 32 ????????
    token-secret: ${OA_TOKEN_SECRET:please-change-in-prod}
  crypto:
    # 16/24/32 ?? AES ??
    aes-key: ${OA_AES_KEY:please-change-in-prod-16b}
```

### 5.2 ????
- ???????? `token-secret` ??`aes-key`
- ??????????????????/ Vault??
- ?? HTTPS????Nginx ????
- ??????80/443 ????306 ????

---

## ???????

### 6.1 ????
```bash
# ???? 3 ??????
mysqldump -uroot -p cursor | gzip > /backup/cursor_$(date +\%Y\%m\%d).sql.gz
```

### 6.2 ??
- ?????`./logs/ops-platform-module-oa.log`??????
- ?????? `oa_audit_log`??????80 ??

### 6.3 ????????
- Prometheus + Grafana ?? `/actuator/prometheus`
- ?????JVM ?? / SQL ????/ ?? P95

### 6.4 ?????S5 ????
- ?????avg 0.48ms / P95 0.73ms
- ???????avg 0.74ms / P95 0.80ms
- ??????avg 0.45ms / P95 0.76ms
- ???????avg 0.36ms / P95 0.50ms

---

## ???????

```bash
# 1. ????
systemctl stop yudao-oa

# 2. ???? jar
cp /opt/yudao/ops-platform-module-oa.jar /opt/yudao/backup/

# 3. ?? jar
cp /opt/yudao/backup/ops-platform-module-oa-1.0.0-prev.jar /opt/yudao/

# 4. ?? DB?????
mysql -uroot -p cursor < /backup/cursor_prev.sql

# 5. ??
systemctl start yudao-oa
```

---

## ???????Checklist

| ?? | ???? |
|------|----------|
| 401 ???? | token-secret ?????/ ?????? SHA-256 vs ??PBKDF2??|
| ???? 1511 | ???`@RateLimit` ?? / IP ?? |
| ????????| cron ????/ ?????ENABLED |
| ?????? | ?? score_standard ????????/ ????100 |
| ?????? | ???????PENDING ?? approve/reject |

---

## ??ADR ??

- ADR-001 ????Redis / MQ / MinIO / XXL-JOB
- ADR-002 ??????????
- ADR-003 ????? TenantContext ThreadLocal
- ADR-004 ?? PBKDF2?S5 ????

????`docs/engineering/ADR-*.md`??
