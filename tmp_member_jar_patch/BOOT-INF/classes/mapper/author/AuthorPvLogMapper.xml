<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="football.module.member.dal.mysql.author.AuthorPvLogMapper">

    <!-- AuthorPvLogMapper.xml -->
    <select id="getAuthorDailyUv" resultType="football.module.member.api.author.dto.AuthorDailyUvSummaryDTO">
        SELECT
            DATE(create_time) AS bizDate,
            COUNT(DISTINCT user_id) AS uv
        FROM author_pv_log
        WHERE
            author_id = #{authorId}
          AND create_time >= #{startTime}
          AND create_time &lt;= #{endTime}
        GROUP BY DATE(create_time)
        ORDER BY bizDate
    </select>
</mapper>